#include <bits/stdc++.h>
#include <functional>
using namespace std;

int main()
{

    set<int> myset = {6, 8, 1, 2, 2, 10, 7};
    for (const auto &x : myset)
    {
        cout << x << endl;
    }

    return 0;
}